spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.cafeqr: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.security: INFO

# Seed an initial platform admin on startup (dev only). Disable in prod.
app:
  bootstrap:
    enabled: true
    admin-email: admin@cafeqr.local
    admin-password: Admin123!
    admin-full-name: Platform Admin
